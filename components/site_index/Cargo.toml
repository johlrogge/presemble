[package]
name = "site_index"
version = "0.20.0"
edition = "2024"

[package.metadata.polylith]
interface = "site_index"

[dependencies]
schema = { workspace = true }
template = { workspace = true }

[dev-dependencies]
tempfile = "3"
