[package]
name = "site_index"
version = "0.12.0"
edition = "2024"

[package.metadata.polylith]
interface = "site_index"

[dependencies]
schema = { workspace = true }

[dev-dependencies]
tempfile = "3"
