[package]
name = "macros"
version = "0.35.0"
edition = "2024"

[package.metadata.polylith]
interface = "macros"

[dependencies]
forms = { workspace = true }

[dev-dependencies]
reader = { workspace = true }
